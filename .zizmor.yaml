# yaml-language-server: $schema=https://raw.githubusercontent.com/woodruffw/zizmor/main/support/zizmor.schema.json
rules:
  unpinned-uses:
    config:
      policies:
        LouisBrunner/.github/*: any
        "*": hash-pin
